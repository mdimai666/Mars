@namespace Mars.Forms.Front.Editors

@* Встроенный редактор короткой строки *@

<FluentTextField Value="@(Binding.ValueAt(Index) as string)"
                 ValueChanged="@(v => Binding.SetValueAt(Index, v))"
                 Disabled="@Binding.ReadOnly"
                 Required="@Binding.Field.Required"
                 Appearance="@FluentInputAppearance.Filled"
                 Class="w-100" />

@code {
    [Parameter] public FormFieldBinding Binding { get; set; } = default!;

    /// <summary>Индекс элемента множественного поля; -1 — значение целиком</summary>
    [Parameter] public int Index { get; set; } = -1;
}
