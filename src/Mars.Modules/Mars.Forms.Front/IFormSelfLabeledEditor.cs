namespace Mars.Forms.Front;

/// <summary>
/// Редактор сам рисует подпись и описание поля — общий рендерер (<c>FormFieldRow</c>) их не
/// дублирует. Маркер нужен доменным редакторам, которые пришли из своих стеков со своей
/// разметкой подписи (например, инлайн-редактор значения метаполя).
/// </summary>
public interface IFormSelfLabeledEditor;
